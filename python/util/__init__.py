"""
This package contains the modules for all python scripts.

To use this package, you can create a symbolic link to the
`util` module in the python script.

For example, to create a symbolic link in the `structure` module,
you can run the following command:
```bash
ln -s ~/my-configs/python/util ~/my-configs/python/structure/util
```

- structure - The module responsible for creating and managing the file structure.
"""
