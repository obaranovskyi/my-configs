" Note: In docs is <C-d> is mapped
let g:VM_maps = {}
let g:VM_maps['Find Under'] = '<C-t>' " replace C-n
let g:VM_maps['Find Subword Under'] = '<C-t>' " replace visual C-n

" to select one by one
" click <C-t> multiple times

" to find and replace:
" <C-t> 9mj
